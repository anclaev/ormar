{
  "name": "auth-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/auth-service/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "auth-service:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "auth-service:build:development"
        },
        "production": {
          "buildTarget": "auth-service:build:production"
        }
      }
    },
    "docker-build": {
      "dependsOn": ["build"],
      "command": "docker build -f apps/auth-service/Dockerfile . -t anclaev/ormar-auth"
    },
    "container": {
      "executor": "@nx-tools/nx-container:build",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "engine": "docker",
        "metadata": {
          "images": ["auth-service"],
          "load": true
        }
      },
      "configurations": {
        "local": {
          "tags": ["anclaev/ormar-auth:local"],
          "push": false
        },
        "development": {
          "tags": ["anclaev/ormar-auth:dev"],
          "push": true,
          "cache-from": ["type=gha"],
          "cache-to": ["type=gha,mode=max"]
        },
        "production": {
          "tags": ["anclaev/ormar-auth:latest"],
          "push": true,
          "cache-from": ["type=gha"],
          "cache-to": ["type=gha,mode=max"]
        }
      }
    }
  }
}
